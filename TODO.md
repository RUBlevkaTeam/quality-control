# TODO — E-CUP quality-control

## 🔴 Критический путь (к пакетной отправке)

| # | задача | статус |
|---|--------|--------|
| 1 | Строгие фолды LoRA на сервере (5 шт) | 🔄 идёт, 3/5 |
| 2 | W&B: логин на сервере и маке | ⬜ нужен ключ |
| 3 | Честная смесь текст+LoRA под строгим протоколом | ⬜ после п.1 |
| 4 | `make_blend_config` → lora_blend.json (веса+пороги) | ⬜ |
| 5 | `verify_lora_merge` — эквивалентность peft/ручного | ⬜ |
| 6 | Скачать adapter_all+конфиг → архив → e2e → тайминг Check | ⬜ |
| 7 | **ПАКЕТНАЯ ОТПРАВКА**: смесь + шаблонный перенос | ⬜ |

## 🟡 После отправки

- ⬜ Прочитать ΔLB как замер покрытия переноса (план: скачок сверх ожидаемого = перенос работает)
- ⬜ Комментарии: few-shot образцы в промпт генерации (решает ручную оценку)
- ⬜ Zero-shot с полными официальными правилами (30 мин GPU) → если 0.22 → 0.4+, переучить LoRA с промптом v2
- ⬜ Абляция картинок ПО КАТЕГОРИЯМ: fire без картинок против fire с картинками (соло LoRA fire 0.661 < текст 0.696 — картинки fire, похоже, вредят; если подтвердится, fire в контейнере текстом-only = экономия тайминга). Визуальные инвестиции — только в БАД (LoRA 0.9306 > текст 0.9233, правило «или на изображении»)
- ⬜ Перепроверить параметры Optuna под строгим протоколом

## ✅ Готово

- Строгий протокол валидации (families_strict), объяснён разрыв CV↔LB
- LoRA-конвейер: 5 family-фолдов + финальный адаптер, фикс разметки
- Шаблонный перенос: слой в run.py, LOO-точность 99%
- Комментарии: цитаты карточки, конкретность 16.5→31.6%
- Официальные правила хакатона в промптах (train==inference)
- W&B-обвязка в коде (runlog.py, безопасный no-op)

## ❌ Закрыто измерениями — не возвращаться

ArcFace на семействах · трансдукция · e5 · LinearSVC · доразметка меток ·
zero-shot/few-shot с кратким промптом · CatBoost ПОЛНОСТЬЮ (соло −0.034; с текстом был +0.010, но в трёхканальной с LoRA — fire −0.037, среднее −0.020, замер 29.08: LoRA поглотила его сигнал) ·
калибровка перед смесью · quantile/максимин-порог · LLM-синтетика позитивов ·
RandomForest (29.08: соло fire 0.49–0.56 против 0.70 у логрега, в смеси с LoRA −0.036/−0.011 — деревья закрыты вторым гвоздём после CatBoost) ·
сопоставление test↔train ОКОНЧАТЕЛЬНО (30.08, три нуля: точные шаблоны ΔLB +0.00008, SHA картинок молчит, kNN-эмбеддинги при гейте 0.98/единогласие — ноль изменённых вердиктов, LB бит-в-бит; слой kNN оставлен в пакете как бесплатная ставка на private) ·
удаление 38 правил (строгий OOF: правила дают fire +0.044, БАД +0.014 — оставить) ·
OCR упаковки ПОЛНОСТЬЮ (fire: OCR читает правду, метка ей не следует; БАД, пилот 400 ошибок 28.08: FN чинится 2/140, у FP упаковка пишет «dietary supplement» при label=0 — спортпит-политика; потенциал +0.000x) ·
перенос порога fire (плато 0.25–0.85 плоское, боевой 0.6724 в −0.005 от оптимума; Липтон F*/2 согласен) ·
ревизия ключа шаблона 220 симв. (150/300 меняют fire ≤0.005 — протокол робастен; шум разбиения фолдов ≈0.01)

## 💡 Предложения (не действия)

- Трёхканальная смесь текст+LoRA+CatBoost, если строгая двухканальная < +0.02
- Зонд Gemma-4-E4B: 1 строгий фолд по БАД против Qwen (фолды БАД стабильны, сравнение осмысленно); полный свап базовой модели не окупается — потолок в разметке, не в модели
- Комментарии: валидатор связности + детерминированный фолбэк из ecup_quality_ultra/src/ultra_explain.py — ✅ внедрено 29.08 вместе с few-shot и официальными правилами в генерации
- [GPU-зонды ЗАКРЫТЫ: 2 эпохи 0.9240, MLP-слои 0.9183 — против базы 0.9328; и ёмкость, и длительность обучения на нашем объёме только переобучают. Остался незамеренным лишь оверсэмплинг fire ×6 — низкий приоритет]
- [Optuna текста ЗАКРЫТ 29.08: fire +0.06 соло / БАД +0.005 соло на свежих сидах, но В СМЕСИ с LoRA +0.000 обе категории — LoRA поглощает текстовые приросты (как и CatBoost). Параметры сохранены (optuna_text_fire/bad.json) как лучший запасной текст при отказе LoRA-канала. Бэггинг тоже закрыт: все варианты ≤ базы]
- Урок дня: улучшать каналы, КОНКУРИРУЮЩИЕ с LoRA, бесполезно — смесь уже всё выжала; шанс имеют только изменения САМОГО LoRA-канала (правила v2, MLP-слои) и слои после смеси
- Эксперимент-сабмит: суп 5 фолдовых адаптеров против adapter_all (честной CV нет по построению, читается только ΔLB; ставка на дисперсию fire)
- Из code-review 29.08, отложенное: одна загрузка 4B вместо двух (снять/навесить LoRA-хуки, ~1–3 мин тайминга) · KV-кэш общего few-shot префикса (генерация в разы дешевле) · токенизировать промпты один раз (сортировка уже по готовым длинам) · батчевый image retrieval вместо iterrows · выпилить мёртвый e5-канал из text_model/run.py (мина: артефакт с e5_head без модели в контейнере молча сместит масштаб вероятностей) · константы вердиктов/порогов dHash в одно место
- A/B комментариев Gemma против Qwen — первый раунд 28.08: скорость сопоставима (9с против 6с на 10 шт), Gemma цитирует карточку лучше, но инвертирует логику вердикта (цитирует «БАД» при вердикте «не БАД») — пока Qwen; финальный A/B после few-shot промпта на обеих
- LoRA-soup: усреднение чекпоинтов/адаптеров против одиночного adapter_all (дисперсия fire 0.51–0.72 по фолдам; model soups — стандартная техника)
- Visual kNN на Qwen3-VL-Embedding-2B (уже в /shared_models) вместо SHA/dHash; перезамер строгим протоколом (+0.015 было под старым)
- Спросить организаторов: критерии ручной оценки комментариев; доля public/private
- Инженерное: одна загрузка 4B вместо двух (классификация+генерация) — снять риск тайминга
